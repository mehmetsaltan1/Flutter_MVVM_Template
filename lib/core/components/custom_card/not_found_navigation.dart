import 'package:flutter/material.dart';

class NotFoundNavigationPage extends StatelessWidget {
  const NotFoundNavigationPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("Not Found Page"),
    );
  }
}
